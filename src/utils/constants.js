export const containerSetting = {
  containerSelector: '.container',
  containerListUpClass: 'container_lift-up',
};

export const startSetting = {
  formSelector: '#start-form',
  startBtnSelector: '.btn[data-type="start"]',
  btnActiveClass: 'btn_type_inactive',
  inputSelecor: '.start__input',
};

export const gameplaySetting = {
  formSelector: '#gameplay-form',
  btnResetSelector: '.btn[data-type="reset"]',
  inputSelector: '.gameplay__input',
  inputGuessSelector: 'gameplay__input_guess',
  pickBtnSelector: '.btn[data-type="pick"]',
  btnInactiveSelector: 'btn_type_inactive',
  infoSelector: '.gameplay__wrapper',
  decorSelector: '.decor[data-type="decor"]',
  decorShowClass: 'decor_show',
  countHint: 0,
};

export const countSetting = {
  counterSelector: '.moves__count',
}
